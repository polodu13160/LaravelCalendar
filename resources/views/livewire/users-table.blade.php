{{-- <div>
    <form action="">
    <input list="options" id="choix" name="choix" wire:model="search" wire:keydown.debounce.500ms="updateSearch">
    <datalist id="options">
        @foreach($users as $user)
            <option value={{ $user->email }}>
        @endforeach
    </datalist>





    {{-- <input  type="text" placeholder="Search mails..." wire:model="search" wire:keydown.debounce.500ms="updateSearch" /> --}}
    {{-- </form>

    <button wire:click="increment">increase</button>
    <h1>{{ $count }}</h1> --}}

    {{-- <ul>
        @foreach($users as $user)
            <li>{{ $user->email }}</li>
        @endforeach
    </ul>
    
</div> --}} 



